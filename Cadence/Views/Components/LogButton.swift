import SwiftUI

// LogButton is unused dead code retained only to avoid breaking
// the Xcode project's Compile Sources phase.  Safe to delete once
// the file reference is removed from the project.

struct LogButton: View {
    var action: () -> Void = {}
    var body: some View { EmptyView() }
}
